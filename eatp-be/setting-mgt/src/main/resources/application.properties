spring.application.name=setting-mgt
spring.config.import=optional:configserver:${MAIN_HOST}:${PORT_CENTRAL_CONFIG}
server.port=${PORT_SETTING_MGT}

# DB CONNECTION - MYSQL
spring.datasource.url=jdbc:mysql://${DB_HOST}:3306/${MYSQL_DB_NAME}?createDatabaseIfNotExist=true
spring.datasource.username=${MYSQL_DB_USER}
spring.datasource.password=${MYSQL_DB_PWD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# HIBERNATE LOG
logging.level.org.hibernate.sql=debug
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE